TradePulse Nepal — SEO V4 Data Library

Replace the current GitHub Pages files with this package. Keep the data/ folder.

Changes in this build:
- Professional 2-column dataset cards
- Preview-first-5-rows panel for all 8 datasets
- CSV and Excel (.xlsx) download options for every dataset
- Excel workbooks include Data + Notes sheets
- Combined CSV + Excel download pack
- Cache-busted CSS/JS links to prevent stale styling
- Updated Dataset structured data with CSV and XLSX distributions
- Updated export/import landing pages with previews and dual-format downloads

After deployment, hard refresh once and test all download buttons.
