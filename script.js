const menu=document.querySelector('.menu');const nav=document.querySelector('.navlinks');if(menu&&nav){menu.addEventListener('click',()=>nav.classList.toggle('open'));}
const io=new IntersectionObserver((entries)=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const form=document.querySelector('#feedback-form');if(form){form.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(form);const subject=encodeURIComponent('TradePulse Nepal feedback — '+(fd.get('type')||'General'));const body=encodeURIComponent('Name: '+(fd.get('name')||'')+'\n\nFeedback type: '+(fd.get('type')||'')+'\n\nMessage:\n'+(fd.get('message')||''));window.location.href='mailto:utsavkphuyal@gmail.com?subject='+subject+'&body='+body;});}
const copy=document.querySelector('.copy-btn');if(copy){copy.addEventListener('click',async()=>{const t=document.querySelector('#citation-text').textContent;await navigator.clipboard.writeText(t);copy.textContent='Copied';setTimeout(()=>copy.textContent='Copy',1400);});}
